import {ArrowRight} from 'lucide-react';import {useState} from 'react';import {api} from './api';import {useI18n} from './i18n';import {LocaleControl} from './locale-control';import {BrandMark,Button,Input,navigate} from './ui';
export function Login(){const{t}=useI18n();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState<string>();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError(undefined);try{await api.login({email,password});const requested=new URLSearchParams(location.search).get('returnTo');navigate(requested?.startsWith('/')&&!requested.startsWith('//')?requested:'/',true)}catch(reason){setError(reason instanceof Error?reason.message:t('login.failed'))}finally{setBusy(false)}}return <main className="login-page"><div className="login-wrap"><div className="login-utilities"><LocaleControl/></div><BrandMark/><section className="card login-panel"><form onSubmit={submit}><h1>{t('login.title')}</h1><p className="muted">{t('login.continue')}</p>{error&&<div className="alert error">{error}</div>}<Input label={t('login.email')} type="email" autoComplete="email" autoFocus required value={email} onChange={e=>setEmail(e.currentTarget.value)}/><Input label={t('login.password')} type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.currentTarget.value)}/><Button type="submit" busy={busy}>{t('login.title')} <ArrowRight/></Button></form></section><small className="muted">{t('login.footnote')}</small></div></main>}
