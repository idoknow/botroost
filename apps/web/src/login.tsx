import {Button,Paper,PasswordInput,Stack,TextInput,Title} from '@mantine/core';import {useState} from 'react';import {api} from './api';
export function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{await api.mutate('/auth/login',{email,password});const target=new URLSearchParams(location.search).get('returnTo')||'/';history.replaceState({},'',target);dispatchEvent(new PopStateEvent('popstate'))}finally{setBusy(false)}}return <Paper maw={400} mx="auto" mt="12vh" p="xl" withBorder><form onSubmit={submit}><Stack><Title order={1}>Sign in</Title><TextInput label="Email" type="email" required value={email} onChange={e=>setEmail(e.currentTarget.value)}/><PasswordInput label="Password" required value={password} onChange={e=>setPassword(e.currentTarget.value)}/><Button type="submit" loading={busy}>Sign in</Button></Stack></form></Paper>}
