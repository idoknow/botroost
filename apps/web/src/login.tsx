import {ArrowRight} from 'lucide-react';import {useState} from 'react';import {api} from './api';import {BrandMark,Button,Input,navigate} from './ui';
export function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState<string>();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError(undefined);try{await api.login({email,password});const requested=new URLSearchParams(location.search).get('returnTo');navigate(requested?.startsWith('/')&&!requested.startsWith('//')?requested:'/',true)}catch(reason){setError(reason instanceof Error?reason.message:'Unable to sign in')}finally{setBusy(false)}}return <main className="login-page"><div className="login-wrap"><BrandMark/><section className="card login-panel"><form onSubmit={submit}><h1>Sign in</h1><p className="muted">Continue to Botroost.</p>{error&&<div className="alert error">{error}</div>}<Input label="Email" type="email" autoComplete="email" autoFocus required value={email} onChange={e=>setEmail(e.currentTarget.value)}/><Input label="Password" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.currentTarget.value)}/><Button type="submit" busy={busy}>Sign in <ArrowRight/></Button></form></section><small className="muted">OneBot endpoint cluster console · Authenticated access only</small></div></main>}
