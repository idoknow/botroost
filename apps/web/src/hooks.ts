import {useCallback,useEffect,useState} from 'react';import {api} from './api';
export function useApi<T>(path:string,interval?:number|((data:T|undefined)=>number|undefined),enabled=true){const [data,setData]=useState<T>();const [error,setError]=useState<Error>();const [loading,setLoading]=useState(enabled);const refresh=useCallback(async()=>{if(!enabled)return;try{setData(await api.get<T>(path));setError(undefined)}catch(e){setError(e as Error)}finally{setLoading(false)}},[path,enabled]);useEffect(()=>{if(!enabled){setLoading(false);return}void refresh();const delay=typeof interval==='function'?interval(data):interval;if(delay){const id=setInterval(refresh,delay);return()=>clearInterval(id)}},[refresh,interval,data,enabled]);return{data,error,loading,refresh,setData}}
export function usePath(){const [path,setPath]=useState(location.pathname+location.search);useEffect(()=>{const fn=()=>setPath(location.pathname+location.search);addEventListener('popstate',fn);return()=>removeEventListener('popstate',fn)},[]);return path}
