import '@mantine/core/styles.css';import './styles.css';import {MantineProvider} from '@mantine/core';import {AppShell,Button,Group,NavLink,Stack,Text,Title} from '@mantine/core';import {useDisclosure} from '@mantine/hooks';import {QueryClient,QueryClientProvider,useQuery} from '@tanstack/react-query';import {BrowserRouter,Link,Navigate,Route,Routes,useLocation} from 'react-router-dom';import {api} from './api';import type {Session} from './types';import {Loading,NotFound} from './states';import {Login} from './login';import {Audit,Credentials,EndpointDetail,Endpoints,Members,NodeDetail,Nodes,OperationDetail,Operations,Overview,Providers,Settings} from './pages';
const newClient=()=>new QueryClient({defaultOptions:{queries:{staleTime:15_000,retry:false}}});let activeClient:QueryClient|undefined;const nav=[['Overview','/','workspace:read'],['Endpoints','/endpoints','endpoint:read'],['Nodes','/nodes','node:read'],['Providers','/providers','provider:read'],['Operations','/operations','operation:read'],['Audit','/audit','audit:read'],['Workspace','/workspace/members','workspace:sensitive']];
const has=(session:Session,permission:string)=>permission==='workspace:sensitive'?['member:read','credential:read','settings:read'].some(item=>session.permissions.includes(item)):session.permissions.includes(permission);
function Guard({session,permission,children}:{session:Session;permission:string;children:React.ReactNode}){return has(session,permission)?children:<NotFound/>}
function Shell({session}:{session:Session}){const [opened,{toggle}]=useDisclosure();const location=useLocation();async function logout(){await api.mutate('/auth/logout');activeClient?.clear();history.replaceState({},'','/login');dispatchEvent(new PopStateEvent('popstate'))}return <AppShell header={{height:52}} navbar={{width:220,breakpoint:'sm',collapsed:{mobile:!opened}}} padding="md"><AppShell.Header><Group h="100%" px="md" justify="space-between"><Group><Button hiddenFrom="sm" variant="subtle" onClick={toggle}>Menu</Button><Title order={3}>Botroost</Title><Text c="dimmed">{session.workspace.name}</Text></Group><Group><Text size="sm">{session.user.name}</Text><Button size="xs" variant="subtle" onClick={logout}>Log out</Button></Group></Group></AppShell.Header><AppShell.Navbar p="xs"><Stack gap={2}>{nav.filter(([, ,permission])=>has(session,permission!)).map(([label,to])=><NavLink key={to} component={Link} to={to!} label={label} active={location.pathname===to||to!=='/'&&location.pathname.startsWith(to!)}/>)}</Stack></AppShell.Navbar><AppShell.Main><Routes><Route path="/" element={<Overview/>}/><Route path="/endpoints" element={<Guard session={session} permission="endpoint:read"><Endpoints session={session}/></Guard>}/><Route path="/endpoints/:id" element={<Guard session={session} permission="endpoint:read"><EndpointDetail session={session}/></Guard>}/><Route path="/nodes" element={<Guard session={session} permission="node:read"><Nodes session={session}/></Guard>}/><Route path="/nodes/:id" element={<Guard session={session} permission="node:read"><NodeDetail/></Guard>}/><Route path="/providers" element={<Guard session={session} permission="provider:read"><Providers/></Guard>}/><Route path="/operations" element={<Guard session={session} permission="operation:read"><Operations/></Guard>}/><Route path="/operations/:id" element={<Guard session={session} permission="operation:read"><OperationDetail/></Guard>}/><Route path="/audit" element={<Guard session={session} permission="audit:read"><Audit/></Guard>}/><Route path="/workspace" element={<Navigate to="/workspace/members"/>}/><Route path="/workspace/members" element={<Guard session={session} permission="member:read"><Members session={session}/></Guard>}/><Route path="/workspace/credentials" element={<Guard session={session} permission="credential:read"><Credentials session={session}/></Guard>}/><Route path="/workspace/settings" element={<Guard session={session} permission="settings:read"><Settings session={session}/></Guard>}/><Route path="*" element={<NotFound/>}/></Routes></AppShell.Main></AppShell>}
function Authenticated(){const location=useLocation();const q=useQuery({queryKey:['session'],queryFn:()=>api.get<Session>('/auth/session'),retry:false});if(q.isLoading)return <Loading/>;if(q.error)return <Navigate to={`/login?returnTo=${encodeURIComponent(location.pathname+location.search)}`} replace/>;return <Shell session={q.data!}/>}
function RoutesRoot(){return <Routes><Route path="/login" element={<Login/>}/><Route path="*" element={<Authenticated/>}/></Routes>}
export function ConsoleApp(){const client=newClient();activeClient=client;const isTest=typeof navigator!=='undefined'&&navigator.userAgent.includes('jsdom');return <MantineProvider defaultColorScheme="dark" env={isTest?'test':'default'}><QueryClientProvider client={client}><BrowserRouter><RoutesRoot/></BrowserRouter></QueryClientProvider></MantineProvider>}
