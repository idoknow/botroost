import type {ReactNode} from 'react';import {Table} from './ui';export type Column<T>={header:string;cell:(row:T)=>ReactNode};export const DataTable=<T,>({data,columns}:{data:T[];columns:Column<T>[]})=><Table headers={columns.map(c=>c.header)} rows={data.map(row=>columns.map(c=>c.cell(row)))}/>;
