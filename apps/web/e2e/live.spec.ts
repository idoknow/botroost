import {test,expect} from '@playwright/test';
const email=process.env.BOTROOST_E2E_EMAIL;const password=process.env.BOTROOST_E2E_PASSWORD;const enabled=Boolean(email&&password&&process.env.BOTROOST_E2E_BASE_URL);
test.describe('live operator journey',()=>{test.skip(!enabled,'Set BOTROOST_E2E_EMAIL, BOTROOST_E2E_PASSWORD, and BOTROOST_E2E_BASE_URL');test('login, create fake endpoint, start, audit, persistence, logout',async({page})=>{await page.goto('/login');await page.getByLabel('Email').fill(email!);await page.getByLabel('Password').fill(password!);await page.getByRole('button',{name:'Sign in'}).click();await expect(page.getByText('Operational overview')).toBeVisible();await page.getByRole('link',{name:'Endpoints'}).click();await page.getByRole('button',{name:'Create endpoint'}).click();await page.getByRole('button',{name:'Fake'}).click();const name=`live-fake-${Date.now()}`;await page.getByLabel('Name').fill(name);await page.getByRole('button',{name:'Create',exact:true}).click();await expect(page.getByRole('heading',{name})).toBeVisible();await page.getByRole('button',{name:'Start'}).click();await expect(page.getByRole('heading',{name:'Operation detail'})).toBeVisible();await page.getByRole('link',{name:'Audit'}).click();await expect(page.getByText(/endpoint|operation/i).first()).toBeVisible();await page.reload();await expect(page.getByRole('heading',{name:'Audit events'})).toBeVisible();await page.getByRole('link',{name:'Endpoints'}).click();await expect(page.getByText(name)).toBeVisible();await page.getByRole('button',{name:'Log out'}).click();await expect(page.getByRole('heading',{name:'Sign in'})).toBeVisible();});});
